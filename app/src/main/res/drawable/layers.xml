<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Notes:
    To prevent scaling of images, use <bitmap> inside each item.
    But to use bitmap, it can only refer to actual images and not
    some .xml drawable-->
    <item
        android:drawable="@android:drawable/btn_star_big_on">
    </item>
    <item
        android:left="10dp"
        android:top="10dp"
        android:drawable="@android:drawable/ic_lock_lock">
    </item>
    <item
        android:left="20dp"
        android:top="20dp"
        android:drawable="@android:drawable/btn_plus">
    </item>
</layer-list>