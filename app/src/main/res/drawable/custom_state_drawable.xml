<?xml version="1.0" encoding="utf-8"?>
<!--
StateListDrawables hold multiple drawables which are tied to states of the view.

Android provides some standard states for views such as pressed, selected.
But we can also define our own attributes and tie drawables to those.

Then in the code we can use setImageState(R.attr.attributeName, true) to set
attribute value for the custom attribute defined under declare-styleable.
-->
<!--
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:drawable="@android:drawable/btn_radio"
        app:projection_carplay="true"/>
    <item android:drawable="@android:drawable/arrow_up_float"
        app:projection_aa="true"/>
    <item android:drawable="@android:drawable/ic_dialog_dialer" />
</selector>
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:drawable="@android:drawable/btn_radio"
        app:projection_carplay="true"/>
    <item android:drawable="@android:drawable/arrow_up_float"
        app:projection_aa="true"/>
    <item android:drawable="@android:drawable/ic_dialog_dialer" />
</selector>
